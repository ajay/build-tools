################################################################################

# Print project info at parse time.

################################################################################

-include $(dir $(lastword $(MAKEFILE_LIST)))base.mk

$(info )
$(info PROJECT = $(PROJECT_NAME))
$(info COMMIT  = $(COMMIT))
$(info )

################################################################################
